# ===================================
#  Form Behavior Config
# ===================================

# Record name
name: Model

# Model Form Field configuration
form: $/charles/mailgun/models/model/fields.yaml

# Model Class name
modelClass: Charles\Mailgun\Models\Model

# Default redirect location
defaultRedirect: charles/mailgun/models

# Create page
create:
    title: Create Model
    redirect: charles/mailgun/models/update/:id
    redirectClose: charles/mailgun/models

# Update page
update:
    title: Edit Model
    redirect: charles/mailgun/models
    redirectClose: charles/mailgun/models

# Preview page
preview:
    title: Preview Model
