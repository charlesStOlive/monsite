# ===================================
#  List Column Definitions
# ===================================

columns:
    name:
        label: Nom
        searchable: true
    slug:
        label: Slug
    is_client:
        label: Cloudi pour client ?
        type: switch
    is_client_needed:
        label: Les infos clients sont elle utile ?
        type: switch
    is_logo_needed:
        label: le logo est il utilisé ?
        type: switch
    pivot[url]:
        label: Url
        type: 'partial'
        path: "$/charles/mailgun/models/cloudi/_url_link.htm"
        clickable: false
